# STATUS — build-packaging (07)

Review complete. Mapped the full build/packaging/distribution pipeline: PyInstaller one-file recipe (`build.spec`), dev scripts (`build.sh/.bat`, `pack.bat`, `setup_env.*`), AppImage recipe (Python-into-AppDir, not frozen), and the 5-output CI matrix (Win/macOS/Linux PyInstaller ×2 arch + Linux AppImage ×2 arch) feeding a `dev-build` prerelease. Traced pystray tray wiring (executor thread, 5 .ico states, AppIndicator bundling on Linux, macOS special-cased). Predicted packaging impact for the BRIEF-04 stack options: pywebview (low, localized: a Vite frontend build + datas + exclude-unused-webview-backends), PySide6/Qt (size 100–250 MB; avoid WebEngine), and Tauri/Wails (needs full backend port). Flagged a pre-existing macOS codesign/notarization gap. Report written to docs/ui-review/07-BUILD-PACKAGING.md; no source code modified. Next: hand off the size/CI table and mac signing gap to BRIEF-00 synthesis.
