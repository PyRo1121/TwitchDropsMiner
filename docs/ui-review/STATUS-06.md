# STATUS-06 — i18n & Settings Audit (COMPLETE)

Full report: `docs/ui-review/06-I18N-SETTINGS.md` (5 sections + appendix). Reviewed translate.py, settings.py, utils.py, constants.py, cache.py, gui.py, and all 20 lang files. Translation: a single `Translator` instance (`translate.py:545`) with dotted-path key lookup; 127 canonical keys across 21 languages (Arabic is RTL, CJK/Cyrillic covered), English-back-fill on load via merge_json. Variable interpolation is keyword `str.format`; **no pluralization** and **no locale number/date formatting**. `set_language` is only invoked at startup (`main.py:141`) — language switching is effectively restart-only. Hard-coded audit found 8 untranslated user-visible strings (Help→About/Donate block, the language label itself, device-activation/logging lines). Settings persist to `settings.json` via atomic `.new`+rename; schema has 11 fields with typed wrappers; backward-compat relies on implicit key/type merge with **no version field**; corrupt primary file = startup error + exit 4. Rewrite needs true runtime switching, RTL + font fallback, and explicit settings schema versioning + validation. Report written, no source modified.
